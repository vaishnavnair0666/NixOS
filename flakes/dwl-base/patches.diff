# diff --git a/dwl.c b/dwl.c
# index 1234567..89abcde 100644
# --- a/dwl.c
# +++ b/dwl.c
# @@ -50,6 +50,9 @@
#  #define BORDERPX 1   /* border pixel of windows */
#  #define SNAP     32  /* snap pixel */
#
# +/* gaps between windows */
# +static const int gappx = 10;
# +
#  /* layout(s) */
#  static const Layout layouts[] = {
#  	/* symbol     arrange function */
# @@ -430,10 +433,14 @@ tile(Monitor *m)
#  		if (!VISIBLE(c))
#  			continue;
#  		w = m->ww / n;
# -		resize(c, m->wx + i * w, m->wy, w - (2*c->bw), m->wh - (2*c->bw), 0);
# +		/* apply gaps */
# +		int gapoffset = gappx / 2;
# +		int gaptotal = gappx * (n - 1);
# +		int cw = (m->ww - gaptotal) / n;
# +		int cx = m->wx + i * (cw + gappx);
# +		resize(c, cx + gapoffset, m->wy + gapoffset, cw - (2*c->bw) - gappx, m->wh - (2*c->bw) - gappx, 0);
#  		i++;
#  	}
#  }
#
#
