{
  "layer": "top",
  "position": "top",
  "height": 30,
  "modules-left": ["custom/workspaces", "swaywindow", "launcher"],
  "modules-center": ["clock"],
  "modules-right": ["custom/network", "pulseaudio", "battery", "memory", "cpu", "tray"],

  "tray-position": "right",
  "tray-padding": 6,

  "module-margin-left": 8,
  "module-margin-right": 8,

  "clock": {
    "format": "{:%a %d %b %H:%M}",
    "tooltip": false
  },

  "memory": {
    "format": "RAM {used}/{total} MB",
    "interval": 5
  },

  "cpu": {
    "format": "CPU {usage}%"
  },

  "battery": {
    "format": "{capacity}% {time:hh\\:mm}",
    "format-discharging": " {capacity}% {time:hh\\:mm}",
    "tooltip": true
  },

  "pulseaudio": {
    "format": "{volume}%{?muted: (MUTED)}",
    "format-muted": "Muted",
    "tooltip": true,
    "on-click": "pavucontrol"
  },

  "tray": {
    "max-size": 20
  },

  "swaywindow": {
    "format": "{title}",
    "max-length": 40
  },

  "launcher": {
    "exec": "bash -lc 'wofi --show drun'",
    "return-type": "json",
    "on-click": "bash -lc 'wofi --show drun'"
  },

  "custom/workspaces": {
    "exec": "waybarWorkspace",
    "interval": 1,
    "on-click": "waybarWorkspaceAction"
  },

"custom/network": {
  "exec": "waybarNetwork",
  "interval": 5,
  "return-type": "text"
}
}

